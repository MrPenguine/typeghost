#!/usr/bin/env bash
DIR="$(cd "$(dirname "$0")/../../.." && pwd)"
cd "$DIR"

# Ensure venv exists
if [ ! -d ".venv" ]; then
    python3 -m venv .venv
    .venv/bin/pip install -r requirements.txt
fi

exec .venv/bin/python3 typeghost.py "$@"
